// Typing Props

// Create a React component called UserCard that accepts props:

// name: string

// age: number

// isActive?: boolean (optional)

// Render all of them inside the component.

export type CardProps = {
  name: string;
  age: number;
  isActive?: boolean;
};
